import React from 'react';
import RenderUpdateProfile from './RenderUpdateProfileContainer';

function UpdateProfileContainer({ LoadingComponent }) {
  return (
    <>
      <RenderUpdateProfile />
    </>
  );
}

export default UpdateProfileContainer;
