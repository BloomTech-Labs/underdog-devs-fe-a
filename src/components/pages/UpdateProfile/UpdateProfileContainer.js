import React from 'react';
import RenderUpdateProfile from './RenderUpdateProfile';

function UpdateProfileContainer({ LoadingComponent }) {
  return (
    <>
      <RenderUpdateProfile />
    </>
  );
}

export default UpdateProfileContainer;
