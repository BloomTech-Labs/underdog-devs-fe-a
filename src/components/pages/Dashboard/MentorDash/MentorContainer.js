import React from 'react';
import RenderMentorDash from './RenderMentorDash';

function MentorContainer({ LoadingComponent }) {
  return (
    <>
      <RenderMentorDash />
    </>
  );
}

export default MentorContainer;
